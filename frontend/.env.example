# The SPA calls same-origin /api/v1 in dev; Vite proxies it to the backend.
VITE_API_BASE_URL=/api/v1

# Where the Vite dev proxy forwards /api/* (the running Laravel backend).
# `php artisan serve` defaults to http://127.0.0.1:8000.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
